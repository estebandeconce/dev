name: FastCode
on:
  push:
    branches: [ "testing-gha", "master" ]
  workflow_dispatch:

jobs:
  hand-job:
    runs-on: ubuntu-latest
    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      - uses: actions/checkout@v4

      - name: Imprime Hola, Mundo!
        run: echo Hello, world!
